This is a Timezone Converter that gets the weather for cities in those timezones.

To use it select a time or get your current time, then convert it to get that same time in another timezone, as well as the forecast in that timezone.

This page was created using Claude, and it uses Open-Meteo's weather fetching API and its geocoding API.
